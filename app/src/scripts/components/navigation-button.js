import React from 'react';

class NavigationButton extends React.component {
    render () {
        return (
            <a href='#' className="navigation__button">Find stops near me</a>
        );
    }
}

export default NavigationButton;
